## Backend

###### Run `npm i` to install NPM packages, and then `npm start` to start the Express server

If you do not have `nodemon` installed globally, please `npm i nodemon` before starting the Express server
